<div class="container my-1 mx-5">
  <div class="row">
    <h1 class="p-0 mt-4">My tasks</h1>
    <ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

</div>
  </div>

<%= link_to "Add task", tasks_new_path, class: "p-0 text-decoration-none" %>     <--!check what is the path with rails routes-->
